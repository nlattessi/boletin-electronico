{% extends 'BoletinesBundle:Layout:layout.html.twig' %}
{% block breadcrumb %}Usuarios{% endblock %}

{% block page %}

  <div class="row">

    <div class="col s12 m12 l12">

      <div id="striped" >
        <div class="row mar-top-soft">
          <div class="col s10 m10 l10">
            <h3 class="header">Asignar o editar usuarios a instituciones</h3>

          </div>

              <div class="input-field col s11 mar-top-soft">
                <i class="icon-linear-magnifier prefix"></i>
                <input id="icon_prefix" type="text" class="validate">
                <label for="icon_prefix">Buscar institución</label>
              </div>

        </div>

          <div class="row">
            <div class="col s12">

              <table class="striped">
                <thead>
                  <tr>
                      <th data-field="">Nombre</th>
                      <th data-field="">Usuarios</th>
                      <th data-field="">Acciones</th>
                  </tr>
                </thead>
                <tbody>
                {% for institucion in entities %}
                  <tr>
                      <td><a href="{{ path('institucion_show', {'id': institucion.id}) }}" class="casual">{{ institucion.nombre }}</a></td>
                    <td><span class="new badge">3</span></td>

                    <td>
                      <a href="{{ path('usuario_edit_institucion', {'idInstitucion':institucion.id } )}}" data-position="top" data-delay="25" data-tooltip="Editar usuarios" class="tooltipped btn-small btn waves-effect waves-light"><i class="icon-linear-pencil"></i></a>
                      <a href="{{ path('usuario_new_institucion', {'idInstitucion':institucion.id } ) }}" data-position="top" data-delay="25" data-tooltip="Nuevo usuario" class="tooltipped  btn-small btn waves-effect waves-light"><i class="icon-linear-plus"></i></a>

                    </td>


                  </tr>
                {% endfor %}



                </tbody>
              </table>
            </div>
          </div>
      </div>

    </div><!--  End Tables Section  -->
</div>




{% endblock %}
